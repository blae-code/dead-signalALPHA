# If omitted, the app falls back to same-origin /api requests.
# Set this when frontend and backend are on different origins.
REACT_APP_BACKEND_URL=https://api.dead-signal.example.com

# Dev-only webpack health endpoints
ENABLE_HEALTH_CHECK=false
